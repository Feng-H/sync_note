{
  "name": "audio-transcription-app",
  "version": "1.0.0",
  "description": "音频转录助手 - 支持音频播放和文字同步记录",
  "author": "Your Name",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "electron:dev": "concurrently \"npm run dev\" \"wait-on http://localhost:5173 && cross-env NODE_ENV=development electron .\"",
    "electron:build": "npm run build && cd server && npm run build && cd .. && electron-builder",
    "electron:build:mac": "npm run build && cd server && npm run build && cd .. && electron-builder --mac --arm64"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^9.0.1",
    "react-router-dom": "^7.9.6",
    "rehype-raw": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "^39.2.3",
    "electron-builder": "^26.0.12",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "wait-on": "^9.0.3"
  }
}
